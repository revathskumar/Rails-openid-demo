# Rack OpenID with pape (Demo)

1. rails plugin https://github.com/pelle/oauth-plugin.git
2. bundle install --path vendor
3. cp database.yml.example database.yml
4. bundle exec rake db:migrate
5. bundle exec rails s